# ~/.config/firejail/globals.local

blacklist /usr/local/bin
blacklist /usr/local/sbin

private-tmp

seccomp
caps.drop all
ipc-namespace
machine-id
netfilter

noroot
nogroups
nonewprivs
noinput
noautopulse

noexec ${RUNUSER}
noexec /tmp
